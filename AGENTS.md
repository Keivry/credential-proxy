# AGENTS.md

本仓库用 OpenSpec 管理变更（`openspec/` 目录，`.hermes/skills/` 下有 openspec init 生成的 6 个技能）。

**在创建 / 审查 / 实现 change 之前，先读取 `.hermes/skills/openspec-*/SKILL.md`** —— 它们包含当前 openspec CLI 版本的确切命令语义（store 参数、artifact 顺序、guardrails），以项目生成为准。
