## 1. 采集层落地

- [ ] 1.1 新增 `_metrics.py: MetricsCollector`（单锁批递增：`async with self._lock` 每请求一次护全部计数器与 `recent_events.append`，**锁内禁 `await`**——先锁外收集 `delta` 再锁内 `counters[k]+=delta` + `deque.append`（同步段 <20µs），有界 `asyncio.Queue(maxsize=5)` + `ThreadPoolExecutor(max_workers=1, thread_name_prefix="metrics-writer")` 单 worker 串行**覆盖式** `INSERT ... ON CONFLICT DO UPDATE SET col=excluded.col`（累计快照直接覆盖，单写者避免 `col+excluded.col` 双计；增量方案已废弃，`p95` 计算用独立 `asyncio.to_thread` 不与写盘排队）；深拷贝快照 `{k: dict(v) if isinstance(v,dict) else v}` + `list(deque)` 防线程撕裂；固定 12 桶 `LATENCY_BUCKETS=[10,25,50,100,200,400,800,1500,3000,5000,10000,Inf] ms` 命中 `metrics_bucket`，`recent_events deque(10000)` 每条含 `latency_ms`，返回 `ring_coverage_s/is_precise`（`is_precise=(now-oldest_ts)>=3600 and len>=100`，50 RPS+ 时 1h 永 `≈` 属预期，`len==0` 时 `p95=None`），`dropped_snapshots` + `last_dropped_ts` 暴露到 `health` 且 `QueueFull` 时 `logger.warning`；表 `daily_agg(date TEXT, upstream TEXT, pii_by_type JSON, pii_hits INT, pii_miss INT, cred_hits INT, cred_miss INT, cred_lru_evictions INT, pii_lru_evictions INT, requests INT, requests_by_status JSON, tokens JSON, audit_by_verdict JSON, audit_by_rule JSON, latency_buckets JSON, PRIMARY KEY(date, upstream))` 30天 + `hourly_agg(hour TEXT, upstream TEXT, requests INT, requests_by_status JSON, tokens JSON, latency_buckets JSON, pii_by_type JSON, pii_lru_evictions INT, cred_lru_evictions INT, PRIMARY KEY(hour, upstream))` 7天滑动，每 5min 覆盖 UPSERT + 优雅关闭显式 `await collector.close()`（cancel 定时器 + 最终 flush + 等待 executor + `PRAGMA wal_checkpoint(TRUNCATE)` + 重 `chmod 0600`），`PRAGMA journal_mode=WAL` + `busy_timeout=5000` + `synchronous=NORMAL` + `wal_autocheckpoint=1000` + `user_version=1`，`CREATE INDEX idx_daily_agg_date/hour`，`date=%Y-%m-%d` / `hour=%Y-%m-%dT%H:00:00Z` 统一 `timezone.utc`，文件与 `-wal`/`-shm` 均 `0600`）
  - 验收：`grep -rn MetricsCollector _metrics.py proxy.py` 命中单例且 `asyncio.Lock`/`asyncio.Queue(maxsize=5)`/`ThreadPoolExecutor(max_workers=1)` 存在；`grep -rn "DO UPDATE SET col=excluded.col" _metrics.py` 命中且无 `col+excluded.col`；`grep -rn LATENCY_BUCKETS _metrics.py` 命中 12 桶；`metrics.sqlite` 不存在时首次 `GET /_admin/metrics` 自动建表不报错；`PRAGMA journal_mode` 为 WAL，`PRAGMA user_version==1`，`busy_timeout=5000`；`ls -l metrics.sqlite*` 均为 0600；`grep -rn "dropped_snapshots" _metrics.py` 命中且 `grep -rn "last_dropped_ts" _metrics.py` 命中；`grep -A2 "async with self._lock" _metrics.py | grep -c "await"` 为 0（锁内禁 await）；连续两次 5min flush 后 `SELECT requests,requests_by_status` 与内存累计一致不翻倍；`kill -TERM` 后最近 5min 快照仍可查（`wal_checkpoint` 生效）

- [ ] 1.2 PII/凭据埋点（口径拆分）：`_pii.py:PiiDetector.scan` → `pii_detected_total{kind}`；`_token.py:GlobalPiiTokens.register` → `pii_cache_hit/miss`（命中复用/新建，响应侧 `resp_p2t` 不参与；淘汰分支 `while len(table_p2t)>PII_MAX_ENTRIES: popitem(last=False)` 时 `pii_lru_evictions+=1`（两表 `pii_p2t/resp_p2t` 各自触发均 +1））；`TokenMixin._register_secret` + `_llm.py._redact` 快照命中 → `cred_hit/miss`（按请求 `out!=in` 计 1，单锁批递增） + `cred_lru_evictions`（`len(pwd_to_token)>=MAX_TOKEN_ENTRIES` 分支内 `next(iter(pwd_to_token))` 取最老并 `pop(oldest)` 时 `+1`）；集中 `sanitize_kind`（`len>32|__|\\x00|大小写归一不在内置/自定义白名单→custom_other`，`audit_by_rule` 存 `reason`）三处落盘前必经
  - 验收：同值连续两次：第一次 `detected+1 miss+1`，第二次 `detected+1 hit+1`；凭据达 `MAX_TOKEN_ENTRIES` 淘汰时 `cred_lru_evictions+1`，PII 达 `PII_MAX_ENTRIES` 淘汰时 `pii_lru_evictions+1`；注入 `PII_CUSTOM_PATTERNS` 超长名后 `pii_by_type` 只出现 `custom_other`；`grep -rn sanitize_kind _metrics.py _pii.py _token.py` 命中三处；`grep -rn pii_lru_evictions _token.py _metrics.py` 命中

- [ ] 1.3 上游/Token/守门埋点：`_llm.py` handler 抽 `is_chat_tail(tail)`（`chat/completions|v1/messages|v1/responses`），`upstream` 主键仅 `port`（`tail/stream` 为事件维度不进主键；`/v1/models` 等非对话归 `other` 不丢 `requests_total`）、`requests_total{status}`（每条请求计 1，重试不计多次，落盘 `requests INT + requests_by_status JSON`）、`latency_ms/ttft/bytes_in/out` + 固定 12 桶 `latency_buckets`（`LATENCY_BUCKETS=[10,25,50,100,200,400,800,1500,3000,5000,10000,Inf] ms` 逆分位得 `p95≈`）、`empty_guarded`（流式 `bytes_written==0 && 200` 注入与非流式 `not resp_text.strip() && 200` 转 `502` 两条路径均 +1）、token `usage` 归一（OpenAI/Anthropic/Responses 有则记无则 `unknown`）、`client_gone` + `exception` 状态（统一 `try/finally` + `except (ClientConnectionError, ServerDisconnectedError, TimeoutError, SSE_CLIENT_GONE)`）；**流式与非流式两条响应路径均埋点**（非流式 `upstream_resp.read()` 分支也记 `requests_total/latency/bytes`）；`recent_events` 存 `latency_ms` 支撑 `1h` p95，`hourly_agg/daily_agg` 存 `latency_buckets` 支撑 `24h≈/7d≈/30d≈`（`1h` 的 `p95 sorted` 放 `asyncio.to_thread` 独立于写盘，`24h/7d/30d` 的 `≈` 由桶 `SUM` 单条 `SELECT ... WHERE hour>=?` 后内存分组逆分位，避免 N+1）
  - 验收：`LLM_8878/8879` 各发请求后 `/_admin/metrics` 按 `upstream` 分组合计等于总量（含 `/v1/models` 归 `other`）；非流式 5xx 也计入 `requests_total` 且 `requests_by_status` 含 `{"200":n,"500":m}`；守门注入单测 `empty_guarded+1`；非流式请求在 `requests_total`/`latency` 可见；`1h` 的 p95 与 ring 现场 `sorted(latency_ms)` 一致且返回 `is_precise`（含 `len>=100` 门限，50 RPS+ 时永 `≈`），`24h/7d/30d` 为 `≈` 近似（桶中位最差 28% @[800,1500)）；非流式空体与流式 `empty_guarded` 均 +1，异常分支 `exception` 状态不丢数；`grep LATENCY_BUCKETS _llm.py` 命中

- [ ] 1.4 审计埋点：`_audit.py:audit_tool_call` 的 `audit_by_verdict`/`audit_by_rule` 分布与 `_append_audit_log` 失败的 `audit_log_write_fail`，`audit_pending_total` 与 `audit_hold_overflows` 为**内存 gauge 必选计数**（暴露到 `GET /_admin/health`，不进 `daily_agg/hourly_agg`）
  - 验收：`block` 命中 `rm -rf` 后 `audit_by_verdict.block+1` 且 `audit_by_rule['危险删除']+1`；写失败注入后 `audit_log_write_fail` 递增；`GET /_admin/health` 含 `audit_pending_total/audit_hold_overflows` gauge

## 2. Admin API

- [ ] 2.1 新增 `_admin.py: AdminMixin` 并在每个 aiohttp app（`_llm.py:_start_one_proxy` 的 `LLM_887x` app 与 `_credential.py:start_credential_api` 的 `8877` app，以及轻量双入口 `llm-proxy-only.py` / `credential-proxy-only.py`）**注册通配 `*` 之前**挂 `/_admin/*`（`metrics?range=1h|24h|7d|30d`、`events?limit&kind&upstream&verdict`、`events/stream SSE`、`health`），所有 runner 共享同一 `MetricsCollector` 单例（`proxy.py` 首行初始化）；**首版不注册 `metrics/prometheus`（404）**，统一 `Cache-Control: no-store, no-cache, must-revalidate, private` + `Pragma: no-cache` + `X-Content-Type-Options: nosniff` + `X-Frame-Options: DENY` + `Referrer-Policy: no-referrer` + `Permissions-Policy: camera=(), microphone=(), geolocation=()`，鉴权中间件置于路由最前（鉴权优先：未鉴权直接 `401` 不触库防时序侧信道，鉴权通过后非法方法才 `405 + Allow: GET`，`HEAD` 显式覆盖防 `aiohttp` 自动降级；`401` body `{"error":"unauthorized"}` + 6 头 `Cache-Control/Pragma/X-Content-Type-Options/X-Frame-Options/Referrer-Policy/Permissions-Policy`）
  - 验收：`curl /_admin/metrics?range=1h|24h|7d|30d` 与 `/_admin/events` 与 `/_admin/health` 均需鉴权；`range=1h` 走内存 ring 精确分位、`24h/7d` 走 `hourly_agg`、`30d` 走 `daily_agg` 的 `latency_buckets` 求和；`POST /_admin/metrics` 无 token 返 `401`（鉴权优先于 405），鉴权后 `POST` 才 `405 + Allow: GET`，`HEAD/OPTIONS/TRACE` 均 `405`；`GET /_admin/metrics/prometheus` 返回 404；遍历 `LLM_8878`、`LLM_8879`、`8877` 三端口 `/_admin/metrics` 均 200 且数值一致；轻量双入口各自 `/_admin/*` 可用

- [ ] 2.2 鉴权与绑定：`OBSERVABILITY_ADMIN_TOKEN` 必填（未设启动 `SystemExit` + `logger.critical`），三选一严格优先级 `X-Admin-Token` 头优先 > `Cookie: __Host-admin_token`（`HttpOnly; Secure; SameSite=Strict; Path=/; Max-Age=3600`，`ENV==dev && ALLOW_LOOPBACK_NO_TOKEN==1` 时 `http` 下回退为普通 `admin_token` 仍 `HttpOnly; Secure(if https); Path=/; Max-Age=3600` 仅去 `__Host-` + `SameSite=Lax` 并黄条，`ENV==prod` 不回退）> `?access_token` 查询参数仅限 `events/stream` SSE 兼容回退（`?access_token` 非空且 `path != /_admin/events/stream` 无条件 `401` 不评估 header/cookie，日志掩码 `access_token`，`admin.html` 换 Cookie 后 `history.replaceState` 清参，反代 `access_log` 过滤；**非 SSE 的 `/_admin/metrics` 与 `/_admin/events` 带 `?access_token` 时 SHALL 返回 401**）；`trust_proxy_headers=false` 不读 `X-Forwarded-For`/`X-Real-IP`/`Forwarded` 且不加载 `Forwarded` 中间件；启动时与 `CREDENTIAL_ADMIN_TOKEN`/`MATRIX_ACCESS_TOKEN`/`DATA_DIR/admin_token` 文件值任一相等（空值短路 `if tok and obs==tok`）则 `SystemExit`；不做裸回环白名单（`ALLOW_LOOPBACK_NO_TOKEN=1 && ENV==dev` 时才放行 `127.0.0.1/::1`/`127.*` 且仅限 `GET` 并打 `warning`，`ipaddress` 精确含 `::ffff:127.0.0.1`）；新 `_admin.py` 单独 `ipaddress` 判定 `127.0.0.0/8|::1|::ffff:127.0.0.1` 不复用 `_credential.py:568` 的 `172.` 过宽逻辑；SSE 同样鉴权且限 `5 concurrent SSE/IP` + `60s :ping` + `5min` 强制断开超限 `429`；`401` 仅 `{"error":"unauthorized"}` 不触 DB
  - 验收：无 token 访问 `/_admin/metrics` 返回 401 且 body 无指标且 `sqlite` 未被查询（日志无 `SELECT`）；加 `X-Admin-Token` 头后 200；`Cookie` 亦 200 且优先级 `header>cookie>query`；用 `CREDENTIAL_ADMIN_TOKEN`/`MATRIX_ACCESS_TOKEN`/`admin_token` 文件值访问均 401；`?access_token` 的 SSE 返回 200 流、无凭证 SSE 401，**`curl "/_admin/metrics?access_token=x" →401 且 `/_admin/events?access_token=x` →401**；`ALLOW_LOOPBACK_NO_TOKEN=1` 未设或 `ENV!=dev` 时回环也 401（负例），`ENV=dev && flag=1` 时 `127.0.0.1` `GET` 放行；`observability-dashboard — 只读与鉴权/健康检查` 全覆盖

- [ ] 2.3 聚合窗口与自动建表：`1h` 走内存 ring 精确 p95（含 `is_precise`，`LATENCY_BUCKETS` 12 桶）、`24h/7d` 走 `hourly_agg` 小时聚合（7天滑动，桶 `SUM` 逆分位 `≈`）、`30d` 走 `daily_agg` 日聚合（含 `latency_buckets` 近似 `≈`，`requests_by_status` 同窗口 `SUM`）；启动时 `metrics.sqlite` 缺失自动建表（WAL+`user_version=1`+索引 `requests_by_status/latency_buckets/pii_lru_evictions` 列），30天/7天滚动删除（Python 计算 `cutoff_date/cutoff_hour` 传入 `WHERE date < ?` / `WHERE hour < ?`，UTC 统一 `timezone.utc`，不依赖 SQLite `date('now')`）；关闭衔接 `proxy.py:shutdown` 在 `runner.cleanup` 前 `await collector.close()`（含 `wal_checkpoint(TRUNCATE)` + 重 `chmod 0600`）+ 轻量入口 `finally: await collector.close()`（覆盖式 UPSERT 无翻倍，`synchronous=NORMAL` 正常退出丢 0/`kill -9` 丢 ≤5min+WAL 未 checkpoint 页）
  - 验收：`rm metrics.sqlite` 后重启首个请求不报错且后续 `?range=7d` 与 `?range=30d` 与预期合计一致且 `30d` 的 `≈` p95 可查（桶中位）；`hourly_agg` 超 7 天行被清理（滚动删除同 TZ）；`daily_agg` 15 列完整含 `pii_hits/miss`/`cred`/`audit` + `hourly_agg` 8 列轻量子集；单条 `SELECT ... WHERE hour>=?` 拉全量后内存分组求和（非 N+1）；`PRAGMA user_version==1`；`kill -TERM` 后最近 1min 仍可查且不翻倍（`wal_checkpoint(TRUNCATE)` 生效），正常退出丢 0

## 3. 单 HTML 实时大盘

- [ ] 3.1 新增 `admin.html` 单文件静态（内联 CSS + **内联 Chart.js ~200KB** + `Chart is not defined` 时 SVG 降级、深色风格，禁用 `onclick` 改 `addEventListener`）挂到 `/_admin/`，首帧 5 KPI（今日请求/脱敏占比/PII 命中/阻断数/p95，p95 标注 `1h精确(低流量≈)/24h≈/7d≈/30d≈`）+ 时序（`1h`/`24h`/`7d`/`30d` 四档，分桶 `LATENCY_BUCKETS` 支撑 `p95≈`）+ 分布（PII 按 kind、上游按 port）三区；首访无 Cookie 时展示居中密码输入框 `type=password`（`autocomplete=current-password`，回车→`fetch('/_admin/health', {headers:{'X-Admin-Token':t}})` 校验，成功由服务端 `Set-Cookie: __Host-admin_token` 写入并 `history.replaceState` 清参，失败抖动），`http` 下 `__Host-` 拒写时 `ENV==dev && ALLOW_LOOPBACK_NO_TOKEN==1` 回退为 `admin_token Lax` 仍 `HttpOnly` 并黄条（`ENV==prod` 不回退），`Chart is not defined` 降级 SVG 且 `try/catch` 不泄露脚本错误
  - 验收：浏览器打开 `/_admin/` 无 Cookie 时见密码框，输入正确 token 后首帧可见 5 卡与两条趋势线，数值与 `/_admin/metrics?range=24h` 合计差值为 0 且 p95 标注为统一文案；`Chart.js` 未定义时图表降级为 SVG 数值仍全；严格 CSP（`script-src 'self'`）下数值表格仍可见；`http` 下 `__Host-` 拒写时见黄条提示

- [ ] 3.2 事件表与过滤：最近事件表（`ts/request_id/upstream/pii_hits/cred_hits/tokens/verdict/latency`，数据源仅 `recent_events`，`audit.log` 仅作 `raw-tail` 排障）支持 `1h|24h|7d|30d` 窗口与 `kind/upstream/verdict` 过滤联动，点击行弹窗看脱敏摘要（`redact_summary(raw,120)` 先脱敏后 `truncate(120)` 单一路径，`[REDACTED:<kind>]` 无明文，注入 `120+64` 长串验证无明文）
  - 验收：`?verdict=block` 过滤后表与 `/_admin/events?verdict=block` 一致；弹窗不含明文 PII（`grep` 明文值命中为 0）

- [ ] 3.3 Live 实时流：前端 `EventSource /_admin/events/stream`（优先 Cookie，仅回退 `?access_token`）推新事件到表首，2 秒内可见；重连间隔 5min 后服务端 `retry` 断开强制用 Cookie 重连；非 SSE 的 `?access_token` 已在 2.2 拒
  - 验收：`Live` 开启下发新请求，完成 2 秒内表首出现新行；无凭证的 SSE 返回 401；`?access_token` 的 SSE 仅在 Cookie 缺失时生效，`/_admin/metrics?access_token=x` 仍 401

## 4. 质量与交付

- [ ] 4.1a 采集层单测：覆盖 `observability-metrics` 4 个 Requirement 的场景（`pii_detected_total`/`pii_cache_hit/miss`/`pii_lru_evictions`/`cred_lru_evictions`、`sanitize_kind custom_other`、`empty_guarded`、`unknown tokens`、非流式路径、`requests_by_status` 分布、`latency_buckets` 12 桶逆分位 `is_precise`/`≈`、`hourly_agg/daily_agg` 窗口一致性、覆盖式落盘不翻倍、`user_version`、`dropped_snapshots` 深拷贝线程安全）
  - 验收：专项单测通过；`grep -rn collector\. _pii.py _token.py _llm.py _audit.py _metrics.py` 覆盖全部埋点文件无遗漏（命中非注释行）；`grep -rn latency_ms` 有 p95 支撑；`grep -rn LATENCY_BUCKETS _metrics.py _llm.py` 命中 12 桶；`grep -rn requests_by_status _metrics.py _llm.py` 命中；`asyncio.gather(100*inc)+executor dumps` 无 `RuntimeError`；连续 flush 不翻倍

- [ ] 4.1b 大盘与鉴权单测：覆盖 `observability-dashboard` 4 个 Requirement（`鉴权 401/405 HEAD/OPTIONS/TRACE`、`header>cookie>query` 优先级且**非 SSE 的 `?access_token` 必 401**、`ALLOW_LOOPBACK` 双条件负例、`is_loopback ipaddress`、`401 不触库`、`health` 含 `ring/dropped/pending`、密码输入框与 `history.replaceState`、CDN/CSP 降级、SSE 2秒与无凭证 401、脱敏无明文 `120+64` 长串）
  - 验收：鉴权/只读/`health`/大盘/输入框/CSP/SSE 专项单测通过；含 `llm-proxy-only.py` / `credential-proxy-only.py` 双入口专项——两个轻量入口各自 `/_admin/*` 可用且鉴权生效（`subprocess: LLM_8878=... OBSERVABILITY_ADMIN_TOKEN=x python llm-proxy-only.py & curl -H X-Admin-Token:x localhost:8878/_admin/health → 200`）；`curl "/_admin/metrics?access_token=x"` 与 `/_admin/events?access_token=x` 均 401

- [ ] 4.1c 回归全绿：全量 `pytest -q` + `ruff check . && ruff format --check .` 0 error，`grep -rn "async with self._lock" _metrics.py` 批递增仅一处且 `grep -A2 "async with self._lock" _metrics.py | grep -c "await"` 为 0，`busy_timeout=5000` 与 `wal_checkpoint(TRUNCATE)` + 重 `chmod 0600` 生效验证，`is_precise` 含 `len>=100` 门限，`grep "DO UPDATE SET col=excluded.col"` 无 `col+excluded`，`grep LATENCY_BUCKETS` 12 桶一致
  - 验收：`pytest -q` 全 pass；`ruff` 0 error；覆盖式不翻倍回归通过

- [ ] 4.2 文档与部署：更新 `README` 的 `/_admin` 章节（`OBSERVABILITY_ADMIN_TOKEN` 必填说明、与 `CREDENTIAL_ADMIN_TOKEN`/`MATRIX_ACCESS_TOKEN`/`admin_token` 文件独立与空值短路、`?access_token` 仅 SSE 回退且**非 SSE 带参必 401** 需 `history.replaceState` 与反代过滤、`admin.html` 密码输入框与 `__Host-`/`ENV==dev` 回退、数据留存 30天/7天（含 `requests_by_status`/`latency_buckets 12 桶`/`pii_lru_evictions`）、WAL `0600`+`user_version`、`synchronous=NORMAL 断电丢≤5min`、p95 `≈` 含义（桶中位）、`ENV` 取值、不含明文声明与 `redact→truncate` 单一路径、覆盖式 UPSERT 不翻倍）、`docker-compose.yml`/`docker-entrypoint.sh`（新增必填 env、`${DATA_DIR:-./data}:/data` 卷挂载与子 `tpm:ro/db:ro` 叠加验证、`mkdir -p /data && chmod 700 /data && chown...` + `entrypoint chmod 700 "$DATA_DIR"` 闭环 `umask 022`，`metrics.sqlite` 及其 `-wal`/`-shm` 0600 初始化与 `wal_checkpoint` 后重 chmod `os.umask(0o077)` 窗口期防护）、镜像验证回滚可删 `metrics.sqlite`
  - 验收：`README` 含 `/_admin` 鉴权与隐私声明（含密码框与 `?access_token` 非 SSE 401）；`docker compose config` 可见新增 env 与 `/data` 卷且 `tpm:ro/db:ro` 仍 ro；所有 `*_887x` 端口均暴露 `/_admin` 需同策略防火墙声明；回滚镜像后服务正常且可删 `metrics.sqlite` 不影响启动；`ls -ld /data` 为 700；`README` 含 `LATENCY_BUCKETS` 12 桶误差表（最差 28% @[800,1500)）与 `requests_by_status` 及 `≈`/`SSE 5/IP` 说明
