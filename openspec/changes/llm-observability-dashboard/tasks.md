## 1. 采集层落地

- [x] 1.1 新增 `_metrics.py: MetricsCollector`（单锁批递增：`async with self._lock` 每请求一次护全部计数器与 `recent_events.append`，**锁内禁 `await`**——先锁外收集 `delta` 再锁内 `counters[k]+=delta` + `deque.append`（同步段 <20µs，拷贝段 100~400µs 每 5min 可接受，深拷贝 `{k: dict(v) if isinstance(v,dict) else v}` + `list(deque)` 约 1~2MB 防线程撕裂，覆盖式快照丢弃不丢数（同窗口内不丢数，跨hour/date边界可能丢增量，需按窗口键落盘旧键/清零当前桶）），有界 `queue.Queue(maxsize=5)`（线程安全，替代 `asyncio.Queue` 跨线程不安全，或经 `loop.call_soon_threadsafe` 入队） + `ThreadPoolExecutor(max_workers=1, thread_name_prefix="metrics-writer")` 单 worker 串行**覆盖式** `INSERT ... ON CONFLICT DO UPDATE SET col=excluded.col`（累计快照直接覆盖，单写者避免 `col+excluded.col` 双计；增量方案已废弃，`p95` 计算用独立专用 `ThreadPoolExecutor(max_workers=1, thread_name_prefix="p95-worker")`（或等效隔离 `asyncio.to_thread` 专用池）**独立于** `metrics-writer` 不与写盘排队，`QueueFull` 时 `get_nowait` 丢最老再入队并计 `dropped_snapshots` + `first_dropped_ts/last_dropped_ts`（覆盖式不丢数，`logger.warning`），磁盘满 `ENOSPC` 降级内存-only（恢复后仅从当前累计续写，不回补） 且 `health.sqlite_ok=false` 不拖垮代理）；固定 12 桶 `LATENCY_BUCKETS=[10,25,50,100,200,400,800,1500,3000,5000,10000,Inf] ms` 二分命中 `metrics_bucket`；`recent_events deque(10000)` 每条含 `latency_ms`，返回 `ring_coverage_s/is_precise`（`is_precise=(now-oldest_ts)>=3600 and len>=100`，50 RPS+ 时 1h 永 `≈` 属预期，`len==0` 时 `p95=None`），滚动 `DELETE` 不做 per-flush `VACUUM`（月度 `VACUUM` 或 `auto_vacuum=INCREMENTAL` 二期）；表 `daily_agg(date TEXT, upstream TEXT, pii_by_type JSON, pii_hits INT, pii_miss INT, cred_hits INT, cred_miss INT, cred_lru_evictions INT, pii_lru_evictions INT, requests INT, requests_by_status JSON, tokens JSON, audit_by_verdict JSON, audit_by_rule JSON, latency_buckets JSON, placeholder_prompt_injected INT, truncated_total INT, json_aware_success INT, json_leaf_fallback INT, json_full_fallback INT, PRIMARY KEY(date, upstream))` 30天（15 基础列 + 5 扩展列 `placeholder_prompt_injected`/`truncated_total`/`json_aware_success`/`json_leaf_fallback`/`json_full_fallback`，与 design D1/spec 2.3 一致） + `hourly_agg(hour TEXT, upstream TEXT, requests INT, requests_by_status JSON, tokens JSON, latency_buckets JSON, pii_by_type JSON, pii_lru_evictions INT, cred_lru_evictions INT, PRIMARY KEY(hour, upstream))` 7天滑动，每 5min 覆盖 UPSERT + 优雅关闭显式 `await collector.close()`（cancel 定时器 + 最终 flush + 等待 executor + `PRAGMA wal_checkpoint(TRUNCATE)` + 重 `chmod 0600`），`PRAGMA journal_mode=WAL` + `busy_timeout=5000` + `synchronous=NORMAL` + `wal_autocheckpoint=1000` + `user_version=1`，`CREATE INDEX idx_daily_agg_date/hour`（`PRIMARY KEY(date,upstream)` 已含前缀索引可省），`date=%Y-%m-%d` / `hour=%Y-%m-%dT%H:00:00Z` 统一 `timezone.utc`，建表前 `os.umask(0o077)` 紧邻且 `finally` 恢复，文件与 `-wal`/`-shm` 均 `0600`（每次 UPSERT 后 `glob metrics.sqlite*` 批量 chmod，防 WAL 延迟创建遗漏））
  - 验收：`grep -rn MetricsCollector _metrics.py proxy.py` 命中单例且 `asyncio.Lock`/`queue.Queue(maxsize=5)`（线程安全）/`ThreadPoolExecutor(max_workers=1)` 存在；`grep -rn "DO UPDATE SET col=excluded.col" _metrics.py` 命中且无 `col+excluded.col`；`grep -rn LATENCY_BUCKETS _metrics.py` 命中 12 桶；`metrics.sqlite` 不存在时首次 `GET /_admin/metrics` 自动建表不报错；`PRAGMA journal_mode` 为 WAL，`PRAGMA user_version==1`，`busy_timeout=5000`；`ls -l metrics.sqlite*` 均为 0600；`grep -rn "dropped_snapshots" _metrics.py` 命中且 `grep -rn "last_dropped_ts" _metrics.py` 命中；`grep -A2 "async with self._lock" _metrics.py | grep -c "await"` 为 0（锁内禁 await）；连续两次 5min flush 后 `SELECT requests,requests_by_status` 与内存累计一致不翻倍；`kill -TERM` 后最近 5min 快照仍可查（`wal_checkpoint` 生效）

- [x] 1.2 PII/凭据埋点（口径拆分，口径对照：`pii_detected_total` 按次、`pii_cache_hit/miss` 按值、`cred_hit/miss` 按请求 `out!=in` 计 1，不混用；批量淘汰 `pii_lru_evictions+=n`）：`_pii.py:PiiDetector.scan` → `pii_detected_total{kind}`；`_token.py:GlobalPiiTokens.register` → `pii_cache_hit/miss`（命中复用/新建，**仅请求侧合法值计数——`not value` 空值与 `PII_TOKEN_STR_RE.fullmatch(value)`/`TOKEN_PREFIX in value` 非法 token 形态的提前返回分支不计数**（`_token.py:268-280`），**响应侧 `resp_p2t` 不参与**；淘汰分支 `while len(table_p2t)>PII_MAX_ENTRIES: popitem(last=False)` 时按实际弹出条数批量 `pii_lru_evictions+=n`（两表 `pii_p2t/resp_p2t` 各自触发均累加））；`TokenMixin._register_secret`（`_token.py:479`）+ `_llm.py._redact` 快照命中 → `cred_hit/miss`（按请求 `out!=in` 计 1，单锁批递增，`_redact` 命中亦计 `hit`） + `cred_lru_evictions`（`len(pwd_to_token)>=MAX_TOKEN_ENTRIES` 分支内 `next(iter(pwd_to_token))` 取最老并 `pop(oldest)` 时 `+1`，文案旧称 `popitem(last=False)` 已对齐真代码）；集中 `sanitize_kind`（`len>32|__|\x00|大小写归一不在内置 7 种 +自定义白名单→custom_other`，`audit_by_rule` 存 `reason` 非 `pattern`；**`custom_names` 输入来源：`PiiDetector.custom_patterns` 各 `(?P<name>...)` 捕获名集合，由 `PiiDetector` 暴露，非独立模块变量**）三处落盘前必经；`pii_detected_total` 与 `pii_cache_*` 分离可对账
  - 验收：同值连续两次：第一次 `detected+1 miss+1`，第二次 `detected+1 hit+1`；凭据达 `MAX_TOKEN_ENTRIES` 淘汰时 `cred_lru_evictions+1`，PII 批量淘汰 3 条时 `pii_lru_evictions+3`；注入 `PII_CUSTOM_PATTERNS` 超长名后 `pii_by_type` 只出现 `custom_other`；`grep -rn sanitize_kind _metrics.py _pii.py _token.py` 命中三处；`grep -rn pii_lru_evictions _token.py _metrics.py` 命中；`grep -rn "compare_digest" _admin.py` 命中（Token 时序安全）

- [x] 1.3 上游/Token/守门埋点：`_llm.py` handler 抽 `is_chat_tail(tail)`（对 `tail.rstrip('/')` 后 `endswith(chat/completions|v1/messages|v1/responses)` 避免 `/v1/responses/` 漏判，**当前为 19 处内联需收敛为单一共享函数**（`grep -c "tail.rstrip('/').endswith" _llm.py` =19）），`upstream` 主键仅 `port`（`tail/stream` 为事件维度不进主键；`/v1/models` 等非对话归 `other` 不丢 `requests_total`）、`requests_total{status}`（每条请求计 1，重试不计多次首版重试次数不单独暴露二期可加 `upstream_retry_total`，落盘 `requests INT + requests_by_status JSON` 分布）、`latency_ms/ttft/bytes_in/out` + 固定 12 桶 `latency_buckets`（`LATENCY_BUCKETS=[10,25,50,100,200,400,800,1500,3000,5000,10000,Inf] ms` 二分命中逆分位得 `p95≈`）、`empty_guarded`（流式 `bytes_written==0 && 200` 注入与非流式 `not resp_text.strip() && 200` 转 `502` 两条路径均 +1；**非流式 `_is_invalid_json` 非 JSON 转 502 单列 `invalid_json_guarded`**）、token `usage` 归一（**当前无解析需新增：非流式 `upstream_resp.read()` body JSON 提取，流式 `stream_options.include_usage` 末块或 `usage` delta 聚合**，OpenAI/Anthropic/Responses 有则记无则 `unknown` 不估算；**归一字段：`_metrics.py` 新增 `normalize_usage(obj, protocol)`——OpenAI `usage{prompt_tokens,completion_tokens,total_tokens}` vs Anthropic/Responses `usage{input_tokens,output_tokens}`，输出统一 `{prompt,completion,total,input,output,cached_read,cached_write,unknown}`（8 元，含缓存维度）**；**`total` 语义：`total = obj.get("total_tokens") or obj.get("total") or (input+output)`（Anthropic/Responses 无 `total_tokens`，缺失求和，两值均缺 `unknown`）；`cached_read` null 守卫：`(obj.get("prompt_tokens_details") or{}).get("cached_tokens",0)`**；**缓存命中维度：`cached_read` 映射 OpenAI Chat `usage.prompt_tokens_details.cached_tokens` / Anthropic `usage.cache_read_input_tokens` / Responses `usage.input_tokens_details.cached_tokens`，`cached_write` 仅 Anthropic `usage.cache_creation_input_tokens`（OpenAI/Responses 无则 `0`），两字段归入 `tokens JSON` 供成本核算**；**`tokens JSON` 按 `model` 分桶**（取请求 `body.model`，响应若回 `model`/`usage.model` 则以响应为准，无则 `unknown_model`，聚合内部分桶不新增主键列）；**流式注入 `stream_options: {"include_usage": true}`（不覆盖已设）仅限 OpenAI Chat/Responses 且 `is_stream==true`，Anthropic 严禁注入（`/v1/messages` 严格 Schema 携带即 400）**；**流式提取双链均设 `last_usage`**（OpenAI Chat 末块 `usage` / Responses `response.completed.response.usage` / Anthropic `message_start`+`message_delta` 聚合，`seen_terminal` 后继续读 1 块，fast 链 `_fast_emit_data:4560` 已解析 JSON 提取零额外成本）、`client_gone` + `exception` 状态（统一 `try/finally` + `except (ClientConnectionError, ServerDisconnectedError, TimeoutError, SSE_CLIENT_GONE)` 钩子计 `client_gone` 与对应 `status` 含 `exception` 重试不计多次，`requests_total/latency` 全 tail 计）；**流式与非流式两条响应路径均埋点**（非流式 `upstream_resp.read()` 分支也记 `requests_total/latency/bytes`，`bytes_in/out` 依 `Content-Length` 回退）；**流式 slow/fast 双路径均埋点**（handler 外层统一收集 `requests_total/latency/bytes`，fast 链不得漏计；`sse_events` 按 SSE 事件块计——`event:`/`id:`+`data:` 同块写出 `slow_event_pending`/`fast_event_pending` FIFO 拼装每块计 1；合成终止事件 `_build_truncated_event*`/`_fast_truncated` 单列 `truncated_total` 不计入 `sse_events`）；**JSON-aware 三态计数**（`json_aware_success_total`/`json_leaf_fallback_total`/`json_full_fallback_total`，废弃 `plain str.replace` 口径）；`recent_events` 存 `latency_ms` 支撑 `1h` p95，`hourly_agg/daily_agg` 存 `latency_buckets` 支撑 `24h≈/7d≈/30d≈`（`1h` 的 `p95 sorted` 放独立专用 `ThreadPoolExecutor(p95-worker)` 独立于 `metrics-writer` 写盘单 worker，`24h/7d/30d` 的 `≈` 由桶 `SUM` 单条 `SELECT ... WHERE hour>=?` 拉全量后内存分组逆分位，避免 N+1）
  - 验收：`LLM_8878/8879` 各发请求后 `/_admin/metrics` 按 `upstream` 分组合计等于总量（含 `/v1/models` 归 `other`）；非流式 5xx 也计入 `requests_total` 且 `requests_by_status` 含 `{"200":n,"500":m}`；守门注入单测 `empty_guarded+1`；非流式请求在 `requests_total`/`latency` 可见；**slow/fast 双路径各发流式请求后 `requests_total` 均 +1 且 `sse_events` 合计等于事件块总数；合成终止流后 `truncated_total+1` 且 `sse_events` 不含该合成事件**；`1h` 的 p95 与 ring 现场 `sorted(latency_ms)` 一致且返回 `is_precise`（含 `len>=100` 门限，50 RPS+ 时永 `≈`），`24h/7d/30d` 为 `≈` 近似（桶中位最差约30.4% @[800,1500)（等比约33%））；非流式空体与流式 `empty_guarded` 均 +1，非 JSON 转 502 `invalid_json_guarded+1`，异常分支 `exception` 状态不丢数；`grep LATENCY_BUCKETS _llm.py` 命中；`grep -rn "json_aware_success\|json_leaf_fallback\|json_full_fallback" _llm.py _metrics.py` 命中三态；**缓存命中 token：`normalize_usage` 单测注入 OpenAI `prompt_tokens_details.cached_tokens=50` / Anthropic `cache_read_input_tokens=50` / Responses `input_tokens_details.cached_tokens=50` 均归一 `cached_read=50`，Anthropic `cache_creation_input_tokens=10` 归一 `cached_write=10`，OpenAI/Responses 无 `cached_write` 归 `0`；`tokens JSON` 含 `cached_read/cached_write` 且 `/_admin/metrics` 可查**

- [x] 1.4 审计埋点：`_audit.py:audit_tool_call` 的 `audit_by_verdict`/`audit_by_rule` 分布（**verdict 值域以真实实现为准 `allow/deny` 两值，`block` 模式返回的也是 `deny`，`audit_by_rule` 存 `reason` 非 `pattern`**）与 `_append_audit_log` 失败的 `audit_log_write_fail`；`_request_audit_approval` 返回值单列审批结果分布 `audit_approval_result{approved,rejected,expired,failed}`（内存计数重启归零）；`audit_pending_total` 与 `audit_hold_overflows` 为**内存 gauge 必选计数**（暴露到 `GET /_admin/health`，不进 `daily_agg/hourly_agg`）
  - 验收：`rm -rf` 危险模式命中后 `audit_by_verdict.deny+1` 且 `audit_by_rule['危险删除']+1`（非虚构 `block` 值）；approve 模式审批完成 `audit_approval_result` 对应标签 +1；写失败注入后 `audit_log_write_fail` 递增；`GET /_admin/health` 含 `audit_pending_total/audit_hold_overflows` gauge

## 2. Admin API

- [x] 2.1 新增 `_admin.py: AdminMixin` 并在每个 aiohttp app（`_llm.py:_start_one_proxy` 的 `LLM_887x` app 与 `_credential.py:start_credential_api` 的 `8877` app——**`8877` 仅注册 `/credential` 等具体路由无通配，统一走同一 `init_observability(app, collector)` 接口**，以及轻量双入口 `llm-proxy-only.py` / `credential-proxy-only.py`）**在 `_start_one_proxy` 内部、`add_route('*')` 之前**挂 `/_admin/*`（**注入点实证：`_llm.py:5452-5453` `app = web.Application()` 与 `add_route('*','/{tail:.*}',handler)` 两行紧邻无插桩点且 `handler` 为闭包局部变量，必须改 `_start_one_proxy` 内部**——给其增加 `init_observability: Callable[[web.Application], None]` 参数由调用方传入，或在 `5452-5453` 之间插入注册调用；`8877` app（`_credential.py:147-149`）无通配可直接 `app.router.add_get('/_admin/...', ...)`）（`metrics?range=1h|24h|7d|30d`、`events?limit&kind&upstream&verdict`（**`verdict` 过滤值域为 `allow/deny` 两值**）、`events/stream SSE`、`health`），所有 runner 共享同一 `MetricsCollector` 单例（`_metrics.py` 模块级 `get_collector(DATA_DIR)`，`DATA_DIR` 单一来源 `from proxy import DATA_DIR`——注意 `_audit.py:571` 默认 `''`/`_credential.py:549` 默认 `'.'`/`proxy.py:37` 默认 `BASE_DIR` 三处分叉，统一走 `proxy.DATA_DIR`）；**首版不注册 `metrics/prometheus`（404）**，统一 `Cache-Control: no-store, no-cache, must-revalidate, private` + `Pragma: no-cache` + `X-Content-Type-Options: nosniff` + `X-Frame-Options: DENY` + `Referrer-Policy: no-referrer` + `Permissions-Policy: camera=(), microphone=(), geolocation=()`，Token 比较用 `hmac.compare_digest`（等长摘要比较（sha256 定长摘要，防长度侧信道与不等长 TypeError））防时序侧信道，Cookie `__Host-admin_token` 要求 `Secure(仅https)`/`SameSite=Strict`，`http` 回退 `admin_token` 仍 `HttpOnly` 且 `https` 才 `Secure`，鉴权中间件置于路由最前且仅作用于 `path.startswith('/_admin/')` 的请求（其余路径原样放行）（鉴权优先：未鉴权直接 `401` 不触库防时序侧信道，鉴权通过后非法方法才 `405 + Allow: GET`，`HEAD` 显式 `add_route('*', ...)` 首行 `if method!='GET': 405` 防 `aiohttp` 自动降级；`401` body `{"error":"unauthorized"}` + 6 头 `Cache-Control/Pragma/X-Content-Type-Options/X-Frame-Options/Referrer-Policy/Permissions-Policy`，`401` 前不触 DB 且 `GET /_admin/metrics?access_token=x` 非 SSE 带参恒 `401`）且管理接口 `10/min/IP` 限流超限 `429 + Retry-After`）；三入口 `__init__` 首行调用 `validate_observability_token()`（未设/空值 `SystemExit` + `logger.critical`，与 `CREDENTIAL_ADMIN_TOKEN`/`MATRIX_ACCESS_TOKEN`/`DATA_DIR/admin_token` 文件空值短路 `if tok and obs==tok` 任一相等 `SystemExit`；`OBSERVABILITY_DISABLE=1` 时跳过）与 `self._collector = get_collector(DATA_DIR)`
  - 验收：`curl /_admin/metrics?range=1h|24h|7d|30d` 与 `/_admin/events` 与 `/_admin/health` 均需鉴权；`range=1h` 走内存 ring 精确分位、`24h/7d` 走 `hourly_agg`、`30d` 走 `daily_agg` 的 `latency_buckets` 求和；`POST /_admin/metrics` 无 token 返 `401`（鉴权优先于 405），鉴权后 `POST` 才 `405 + Allow: GET`，`HEAD/OPTIONS/TRACE` 均 `405`；`GET /_admin/metrics/prometheus` 返回 404；遍历 `LLM_8878`、`LLM_8879`、`8877` 三端口 `/_admin/metrics` 均 200 且数值一致；轻量双入口各自 `/_admin/*` 可用

- [x] 2.2 鉴权与绑定：`OBSERVABILITY_ADMIN_TOKEN` 必填且建议 ≥32 字符（未设启动 `SystemExit` + `logger.critical`，比较用 `hmac.compare_digest`（等长摘要，sha256 定长摘要防长度侧信道与不等长 TypeError）防时序侧信道），三选一严格优先级 `X-Admin-Token` 头优先 > `Cookie: __Host-admin_token`（`HttpOnly; Secure(仅https); SameSite=Strict; Path=/; Max-Age=3600`，`http` 下浏览器拒写，`ENV==dev && ALLOW_LOOPBACK_NO_TOKEN==1` 时回退为普通 `admin_token` 仍 `HttpOnly` 且 `https` 才 `Secure`/`http` 不带 `Secure`、`SameSite=Lax` 并黄条，`ENV==prod` 不回退）> `?access_token` 查询参数仅限 `events/stream` SSE 兼容回退（`?access_token` 非空且 `path != /_admin/events/stream` 无条件 `401` 不评估 header/cookie，日志掩码 `access_token`，`admin.html` 换 Cookie 后 `history.replaceState` 清参，反代 `access_log` 过滤且 `admin.html` 禁用 JS 时 URL 参仍残留需反代过滤；**非 SSE 的 `/_admin/metrics` 与 `/_admin/events` 带 `?access_token` 时 SHALL 返回 401**）；`trust_proxy_headers=false` 不读 `X-Forwarded-For`/`X-Real-IP`/`Forwarded` 且不加载 `Forwarded` 中间件；启动时与 `CREDENTIAL_ADMIN_TOKEN`/`MATRIX_ACCESS_TOKEN`/`DATA_DIR/admin_token` 文件值任一相等（空值短路 `if tok and obs==tok`）则 `SystemExit`；不做裸回环白名单（`ALLOW_LOOPBACK_NO_TOKEN=1 && ENV==dev` 时才放行 `request.remote` 经 `ipaddress.ip_address` 精确判定 `127.0.0.0/8|::1|::ffff:127.0.0.1` 且仅限 `GET` 并打 `warning`，`_admin.py` 单独 `def _is_loopback(remote)` 用 `ipaddress` 精确判定且需显式 `ipv4_mapped` 转换（不自动归一）不复用 `_credential.py:568` 的 `172.` 过宽前缀）；SSE 同样鉴权且限 `5 concurrent SSE/IP` + `60s :ping` + `5min` 强制断开超限 `429`，管理接口额外 `10/min/IP` 限流超限 `429 + Retry-After`（`on_disconnect` 清理计数器防泄漏）；静态 `/_admin/` 壳免鉴权（`Cache-Control: public`佐证，`/_admin/metrics|events|stream|health`全量鉴权）；`401` 仅 `{"error":"unauthorized"}` 不触 DB 且 `compare_digest` 比较；`GET` 验 `X-Admin-Token`、`Cookie`、`?access_token` 三路优先级 `header>cookie>query`
  - 验收：无 token 访问 `/_admin/metrics` 返回 401 且 body 无指标且 `sqlite` 未被查询（日志无 `SELECT`）；加 `X-Admin-Token` 头后 200；`Cookie` 亦 200 且优先级 `header>cookie>query`；用 `CREDENTIAL_ADMIN_TOKEN`/`MATRIX_ACCESS_TOKEN`/`admin_token` 文件值访问均 401；`?access_token` 的 SSE 返回 200 流、无凭证 SSE 401，**`curl "/_admin/metrics?access_token=x" →401 且 `/_admin/events?access_token=x` →401**；`ALLOW_LOOPBACK_NO_TOKEN=1` 未设或 `ENV!=dev` 时回环也 401（负例），`ENV=dev && flag=1` 时 `127.0.0.1` `GET` 放行；`observability-dashboard — 只读与鉴权/健康检查` 全覆盖

- [x] 2.3 聚合窗口与自动建表：`1h` 走内存 ring 精确 p95（含 `is_precise`，`LATENCY_BUCKETS` 12 桶二分命中，`p95` 用独立专用 `p95-worker` executor 不与 `metrics-writer` 排队）、`24h/7d` 走 `hourly_agg` 小时聚合（7天滑动，桶 `SUM` 逆分位 `≈`）、`30d` 走 `daily_agg` 日聚合（含 `latency_buckets` 近似 `≈`，`requests_by_status` 同窗口 `SUM(JSON)` 归并）；启动时 `metrics.sqlite` 缺失自动建表（WAL+`user_version=1`+索引 `requests_by_status/latency_buckets/pii_lru_evictions` 列，`PRIMARY KEY(date,upstream)` 已含前缀索引可省；**`daily_agg` 含 5 扩展列 `placeholder_prompt_injected`/`truncated_total`/`json_aware_success`/`json_leaf_fallback`/`json_full_fallback`**），30天/7天滚动删除（Python 计算 `cutoff_date/cutoff_hour` 传入 `WHERE date < ?` / `WHERE hour < ?`，UTC 统一 `timezone.utc`，不依赖 SQLite `date('now')`，滚动 `DELETE` 不做 per-flush `VACUUM`，碎片化靠月度 `VACUUM` 或 `auto_vacuum=INCREMENTAL`）；关闭衔接 `proxy.py:shutdown` 在 `runner.cleanup` 前 `await collector.close()`（含 `wal_checkpoint(TRUNCATE)` + 重 `chmod 0600`）+ **轻量双入口 `llm-proxy-only.py` / `credential-proxy-only.py` 同加 `SIGTERM` handler + `finally: await collector.close()`**（覆盖式 UPSERT 无翻倍，`QueueFull` 丢弃覆盖式不丢数，`synchronous=NORMAL` 正常退出丢 0/`kill -9` 丢 ≤5min+WAL 未 checkpoint 页，磁盘满 `ENOSPC` 降级内存-only（恢复后仅从当前累计续写，不回补） 且 `health.sqlite_ok=false`）
  - 验收：`rm metrics.sqlite` 后重启首个请求不报错且后续 `?range=7d` 与 `?range=30d` 与预期合计一致且 `30d` 的 `≈` p95 可查（桶中位）；`hourly_agg` 超 7 天行被清理（滚动删除同 TZ）；`daily_agg` 15 基础列 + 5 扩展列（`placeholder_prompt_injected`/`truncated_total`/`json_aware_success`/`json_leaf_fallback`/`json_full_fallback`）完整 + `hourly_agg` 9 列轻量子集；单条 `SELECT ... WHERE hour>=?` 拉全量后内存分组求和（非 N+1）；`PRAGMA user_version==1`；`kill -TERM` 后最近 1min 仍可查且不翻倍（`wal_checkpoint(TRUNCATE)` 生效），正常退出丢 0

## 3. 单 HTML 实时大盘

- [x] 3.1 新增 `admin.html` 单文件静态（内联 CSS + **内联 Chart.js ~200KB** + `Chart is not defined` 时 SVG 降级、`try/catch` 包初始化不泄露错误栈、禁用 `onclick` 改 `addEventListener`（所有字段经`textContent`转义防XSS）、`style` 抽 `class`）挂到 `/_admin/`（`admin.html` 单独 `Cache-Control: public, max-age=3600 + ETag`，API 侧 `no-store`），首帧 5 KPI（今日请求/脱敏占比/PII 命中/阻断数/p95，p95 标注 `1h精确(低流量≈或50 RPS+ 永≈属预期)/24h≈/7d≈/30d≈` 含 `is_precise`+`≈` 语义）+ 时序（`1h`/`24h`/`7d`/`30d` 四档，分桶 `LATENCY_BUCKETS` 二分命中支撑 `p95≈` 最差 约30.4%）+ 分布（PII 按 kind、上游按 port）三区；首访经 `fetch('/_admin/health',{credentials:'include'})` 探测无 Cookie（`HttpOnly`对`document.cookie`不可见）时展示居中密码输入框 `type=password`（`autocomplete=current-password`，回车→`fetch('/_admin/health', {headers:{'X-Admin-Token':t}})` 校验，成功由服务端 `Set-Cookie: __Host-admin_token=...; HttpOnly; Secure(仅https); SameSite=Strict; Path=/; Max-Age=3600` 写入（`admin.html` 不写 `document.cookie`）并 `history.replaceState` 清可能残留 `?access_token`，失败抖动 401），`http` 下 `__Host-` 拒写时仅 `ENV==dev && ALLOW_LOOPBACK_NO_TOKEN==1` 回退为 `admin_token`（仍 `HttpOnly` 且 `https` 才 `Secure`/`http` 不带 `Secure`、`SameSite=Lax`）并黄条提示 TLS（`ENV==prod` 不回退仍 401），`Chart is not defined` 降级 SVG 且数值表格始终可见
  - 验收：浏览器打开 `/_admin/` 无 Cookie 时见密码框，输入正确 token 后首帧可见 5 卡与两条趋势线，数值与 `/_admin/metrics?range=24h` 合计差值为 0 且 p95 标注为统一文案；`Chart.js` 未定义时图表降级为 SVG 数值仍全；严格 CSP（`script-src 'self'`）下数值表格仍可见；`http` 下 `__Host-` 拒写时见黄条提示

- [x] 3.2 事件表与过滤：最近事件表（`ts/request_id/upstream/pii_hits/cred_hits/tokens/verdict/latency`，数据源仅 `recent_events`，`audit.log` 仅作 `raw-tail` 排障）支持 `1h|24h|7d|30d` 窗口与 `kind/upstream/verdict` 过滤联动，点击行弹窗看脱敏摘要（`redact_summary(raw,120)` 先脱敏后 `truncate(120)` 单一路径，`[REDACTED:<kind>]` 无明文，注入 `120+64` 长串验证无明文）
  - 验收：`?verdict=deny` 过滤后表与 `/_admin/events?verdict=deny` 一致；弹窗不含明文 PII（`grep` 明文值命中为 0）

- [x] 3.3 Live 实时流：前端 `EventSource /_admin/events/stream`（优先 Cookie，仅回退 `?access_token`）推新事件到表首，2 秒内可见；重连间隔 5min 后服务端 `retry` 断开强制用 Cookie 重连；非 SSE 的 `?access_token` 已在 2.2 拒
  - 验收：`Live` 开启下发新请求，完成 2 秒内表首出现新行；无凭证的 SSE 返回 401；`?access_token` 的 SSE 仅在 Cookie 缺失时生效，`/_admin/metrics?access_token=x` 仍 401

## 4. 质量与交付

- [x] 4.1a 采集层单测：覆盖 `observability-metrics` 4 个 Requirement 的场景（`pii_detected_total` 按次/`pii_cache_hit/miss` 按值/`pii_lru_evictions` 批量 `+=n`/`cred_lru_evictions`、`sanitize_kind custom_other`（`grep sanitize_kind` 三处）、`is_chat_tail rstrip('/')` 判定（19 处内联收敛后单点命中 `grep -c "^def is_chat_tail" _llm.py` =1 且 `grep -c "is_chat_tail(" _llm.py` >=19）、`empty_guarded` 两条路径、`invalid_json_guarded` 非 JSON 转 502、`unknown tokens` 不估算、非流式路径 `requests_total/latency/bytes`、`requests_by_status` 分布、`latency_buckets` 12 桶二分命中逆分位 `is_precise=(now-oldest_ts)>=3600 and len>=100`/`≈` 最差 约30.4%、**流式 slow/fast 双路径均计数且 `sse_events` 按块计（`event:`+`data:` 同块 1 事件）+ 合成终止 `truncated_total` 单列不入 `sse_events`**、**占位符注入 `placeholder_prompt_injected_total` 递增且不计 `pii_detected_total`/`bytes_in` 且禁二次扫描**、**JSON-aware 三态 `json_aware_success/leaf_fallback/full_fallback` 合计等于请求数**、**verdict 值域 `allow/deny` + `audit_approval_result` 审批结果分布**、`hourly_agg/daily_agg` 窗口一致性（`hourly` 9 列轻量子集口径与 `daily` 15+5 列差异经 `SUM(JSON)` 归并）、覆盖式落盘不翻倍（`QueueFull` 丢弃不丢数，连续 flush 计数不翻倍）、`user_version=1`、`dropped_snapshots` + `first_dropped_ts/last_dropped_ts`、`ENOSPC` 磁盘满降级内存-only（`health.sqlite_ok=false`）、**sanitize_kind 注入 `__weird__kind`/超长/含 `\x00` kind 后查 DB `pii_by_type` 仅 `custom_other` 无原始 label**、**跨 hour/date 边界：`QueueFull` 丢弃含旧窗口快照后，旧窗口键仍经补偿 UPSERT 落盘不丢增量**、深拷贝线程安全 `asyncio.gather(100*inc)+executor dumps` 无 `deque mutated`、`p95` 独立 `p95-worker` executor 不与写盘排队）
  - 验收：专项单测通过；`grep -rn collector\. _pii.py _token.py _llm.py _audit.py _metrics.py` 覆盖全部埋点文件无遗漏（命中非注释行）；`grep -rn latency_ms` 有 p95 支撑；`grep -rn LATENCY_BUCKETS _metrics.py _llm.py` 命中 12 桶且 `grep -rn "p95-worker" _metrics.py` 命中（独立 executor）；`grep -rn requests_by_status _metrics.py _llm.py` 命中；`grep -rn "is_chat_tail" _llm.py` 命中且 `grep -rn "rstrip" _llm.py` 命中；`grep -rn "compare_digest" _admin.py` 命中；`asyncio.gather(100*inc)+executor dumps` 无 `RuntimeError: deque mutated`；`ENOSPC` 注入后 `health.sqlite_ok==false` 且进程不崩；连续 flush 不翻倍且 `QueueFull` 注入后 `dropped_snapshots+1` 且 `first_dropped_ts` 非空

- [x] 4.1b 大盘与鉴权单测：覆盖 `observability-dashboard` 4 个 Requirement（`鉴权 401/405 HEAD/OPTIONS/TRACE` 且 `401` 前不触 DB、`header>cookie>query` 优先级且**非 SSE 的 `?access_token` 必 401** 且优先级短路、`ALLOW_LOOPBACK` 双条件负例（`ENV!=dev` 时回环亦 401）、`_is_loopback ipaddress.ip_address` 精确 `127.0.0.0/8|::1|::ffff:127.0.0.1`（`grep ipaddress _admin.py` 命中且 `grep 172. _admin.py` 为 0）、`hmac.compare_digest`（等长摘要）时序安全、`Secure(仅https)` Cookie 回退（`http` 无 `Secure`）、`10/min/IP 429 + Retry-After` 管理接口限流与 `5/IP 60s ping 5min` SSE 限流（`on_disconnect` 清理）、`401` 仅 `{"error":"unauthorized"}` 不含指标、`health` 含 `ring/first_dropped/last_dropped/pending/sqlite_error` + **`placeholder_prompt_enabled/truncated_total/json_*` 新字段**（`ENOSPC` 时 `sqlite_ok=false`）、`events?verdict=allow|deny` 过滤值域、密码输入框 `type=password` 与 `autocomplete=current-password` 与 `history.replaceState` 清参、CDN/CSP `Chart is not defined` SVG 降级且 `try/catch` 不泄露、SSE 2秒与无凭证 401、脱敏无明文 `120+64` 长串含截断边界半字符）
  - 验收：鉴权/只读/`health`/大盘/输入框/CSP/SSE 专项单测通过；含 `llm-proxy-only.py` / `credential-proxy-only.py` 双入口专项——两个轻量入口各自 `/_admin/*` 可用且鉴权生效（`subprocess: LLM_8878=... OBSERVABILITY_ADMIN_TOKEN=x python llm-proxy-only.py & curl -H X-Admin-Token:x localhost:8878/_admin/health → 200`）；`curl "/_admin/metrics?access_token=x"` 与 `/_admin/events?access_token=x` 均 401；`for i in 1..11; do curl -H X-Admin-Token:bad /_admin/health; done` 第 11 次 `429 + Retry-After`；`grep -rn "compare_digest" _admin.py` 命中且 `grep -rn "Secure" _admin.py` 命中且 `http` 回退 Cookie 无 `Secure`；`grep -rn "ipaddress" _admin.py` 命中且 `grep -c "172\." _admin.py` 为 0（不复用过宽前缀）

- [x] 4.1c 回归全绿：全量 `pytest -q` + `ruff check . && ruff format --check .` 0 error，`grep -rn "async with self._lock" _metrics.py` 批递增仅一处且锁内禁 `await` 用 AST 检测（`python -c "import ast; s=open('_metrics.py').read(); i=s.index('async with self._lock'); assert 'await' not in s[i:i+200]"` 替代脆弱的 `grep -A2`，锁块跨多行不漏判），`grep -rn "p95-worker" _metrics.py` 命中（独立 executor），`grep -rn "compare_digest" _admin.py` 命中，`grep -rn "ipaddress" _admin.py` 命中且 `grep -c "172\\." _admin.py` 为 0，`busy_timeout=5000` 与 `wal_checkpoint(TRUNCATE)` + 重 `chmod 0600` 生效验证（`stat -c %a metrics.sqlite* | grep -qx 600` 三文件），`is_precise` 含 `len>=100` 门限，`grep "DO UPDATE SET col=excluded.col"` 命中且无 `col+excluded.col`，`grep LATENCY_BUCKETS` 12 桶二分命中一致，`grep "10/min"` 限流命中，`ENOSPC` 与 `QueueFull` 回归通过
  - 验收：`pytest -q` 全 pass；`ruff` 0 error；覆盖式不翻倍回归通过

- [x] 4.2 文档与部署：更新 `README` 的 `/_admin` 章节（`OBSERVABILITY_ADMIN_TOKEN` 必填（未设/空值 `SystemExit`，长度<32 仅 `logger.warning` 不阻断——与"建议 ≥32"一致）说明、`hmac.compare_digest`（等长摘要）时序安全、与 `CREDENTIAL_ADMIN_TOKEN`/`MATRIX_ACCESS_TOKEN`/`admin_token` 文件独立与空值短路、`?access_token` 仅 SSE 回退且**非 SSE 带参必 401** 需 `history.replaceState` 与反代 `access_log` 过滤（含禁用 JS 残留风险）、`admin.html` 密码输入框 `type=password autocomplete=current-password` 与 `__Host-`/`ENV==dev` 回退（`https` 才 `Secure`/`http` 不带 `Secure`）及黄条 TLS、`ports` 默认 `127.0.0.1:887x:887x` 绑定或宿主防火墙声明（`0.0.0.0` 直出风险）、数据留存 30天/7天（含 `requests_by_status`/`latency_buckets 12 桶二分命中`/`pii_lru_evictions`、`hourly` 9 列轻量子集口径差异、`daily` 15+5 扩展列）、WAL `0600`+`user_version`、`synchronous=NORMAL 断电丢≤5min`、p95 `≈` 含义（桶中位最差约30.4% @[800,1500)（等比约33%））与 `is_precise=(now-oldest_ts)>=3600 and len>=100`、每 5min 覆盖式 UPSERT 不翻倍且 `QueueFull` 覆盖式不丢数、`ENOSPC` 磁盘满降级内存-only 且 `health.sqlite_ok=false`、`ENV` 取 `os.environ.get("ENV","prod")` 取值、不含明文声明与 `redact→truncate` 单一路径及截断边界半字符保护、`SSE 5/IP 60s ping 5min` + 管理接口 `10/min/IP 429` 限流、**`PII_PLACEHOLDER_PROMPT` 开关对指标口径的影响（注入不计 `pii_detected_total`/`bytes_in`，`health.placeholder_prompt_enabled`）**、**`OBSERVABILITY_DISABLE=1` 过渡逃生开关（二期收紧，已入 spec，生产仍必填校验）**、**v0.9.25 基线：slow/fast 双路径埋点与 `sse_events` 按块计 + `truncated_total` + JSON-aware 三态 + verdict 值域 `allow/deny`**）、`docker-compose.yml`/`docker-entrypoint.sh`（新增必填 env `OBSERVABILITY_ADMIN_TOKEN`、`DATA_DIR` 卷挂载 `${DATA_DIR:-./data}:/data` 父卷在前、`tpm:ro/db:ro` 子卷在后顺序即挂载顺序需 `docker compose config` 验证不遮蔽（**若宿主需保留子卷 `ro`，改 `/data` 父卷 + `/data/metrics` 子目录单卷**避免遮蔽）、`mkdir -p /data && chmod 700 /data && chown credential-proxy:credential-proxy /data` + `entrypoint chmod 700 "$DATA_DIR"` 闭环 `umask 022`，`metrics.sqlite` 及其 `-wal`/`-shm` 0600 初始化与 `wal_checkpoint(TRUNCATE)` 后重 chmod `os.umask(0o077)` 窗口期防护且 `finally` 恢复、**镜像版本 `ghcr.io/keivry/credential-proxy` 升至 `0.9.25`**）、镜像验证回滚可删 `metrics.sqlite` 且 `DATA_DIR` 绑 `127.0.0.1` 时 `/_admin` 仍需 TLS 反代
  - 验收：`README` 含 `/_admin` 鉴权与隐私声明（含密码框与 `?access_token` 非 SSE 401）；`docker compose config` 可见新增 env 与 `/data` 卷且 `tpm:ro/db:ro` 仍 ro，且 `ports` 默认绑定 `127.0.0.1:887x:887x`（`grep -n "127.0.0.1:887" docker-compose.yml` 命中，无裸 `"887x:887x"`）；所有 `*_887x` 端口均暴露 `/_admin` 需同策略防火墙声明；回滚镜像后服务正常且可删 `metrics.sqlite` 不影响启动；`ls -ld /data` 为 700；`README` 含 `LATENCY_BUCKETS` 12 桶误差表（最差约30.4% @[800,1500)（等比约33%））与 `requests_by_status` 及 `≈`/`SSE 5/IP` 说明；ENOSPC 注入（`ulimit -f` 或占满 `/data`）后 `health.sqlite_ok==false` 且 `sqlite_error` 非空、进程不崩，恢复后续写不回补
