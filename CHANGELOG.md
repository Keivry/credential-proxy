# Changelog

- **v0.9.23** — Responses API SSE 块结构修复（10.13，线上实测闭环）：修复 `/v1/responses` 流经 slow/fast 双路径转发时 `event:` 行与 `data:` 行被拆成独立块（SSE 协议破坏），下游 OpenAI SDK 按块解析报 `JSONDecodeError: Expecting value: line 1 column 1 (char 0)`、Hermes 重试全灭。① slow 链 `event:`/`id:` 行改暂存（`slow_event_pending`）→ 拼入 data 行同一块写出（`_handle_responses_event` 主路径 + 续行重建路径）；② fast 链对称修复（`fast_event_pending` + `_fast_emit_data` 拼装）；③ `_responses_event` 识别 `ping` 为 'other'（上游 `event: ping\ndata: {"type":"ping"}` 不再拆块）；④ 双链流末 pending 残留透传（异常流不闷 event 行）；⑤ 实测闭环：openai sdk 直连上游 OK（13-20 事件完整）vs 走 proxy JSONDecodeError 0 事件 → 修复后走 proxy 12 事件完整正常

- **v0.9.20** — 复审闭环（§10 10.1-10.12，四子任务并行复审 §9 + 逐条甄别后修复）：① 10.1 续行重建 `delta` 未定义修复（🔴 R-01）——续行路径补 `_agg_delta` 聚合 + `delta = _agg_delta or{}`，主路径 None 哨兵保留首个 delta（R-04 同修）；② 10.2 响应侧 scan 缓存跨 scope 污染（🔴 F-04）——key 加 `id(pii_scope)+_seq` 指纹；③ 10.3 同值不同位置漏掩（🔴 F-05/F-SEC-01）——位置感知替换（仅替换不在 span 内的出现），mutation 验证锁住；④ 10.4 restored_spans 偏移漂移（🔴 F-SEC-02）——`_offset_delta` 累计偏移校正多 token 坐标，mutation 验证锁住；⑤ 10.5 CR-only `[DONE]` 双链对称（🟡 F-01/R-03）——慢链/快链单独透传 + 原子置位 `_done_sent`，新增 `test_integration_cr_only_done`；⑥ 10.6 audit pending 仅抑制纯 tool_calls 行（🟡 F-03/R-05）——混合行 content 不再被连带丢弃；⑦ 10.7 keepalive 审计盲区补强（🟡 F-07）——`_reset_keepalive` 条件补 pending + `_request_audit_approval` 独立保活协程；⑧ 10.8 CR-only 逐条目解析 + chat 流 30s 检查限定非 chat 协议（🟡 F-08）；⑨ 10.9 快链 TTFB 优化（🟡 R-02）——短 content（<64B 无候选）直接透传；⑩ 10.10 性能项评估（P-02 随 10.2 完成，P-03/P-04/P-05 标注可接受）；⑪ 10.11 安全/测试补强——截断 warning preview 双重脱敏（`_strip_partials`+`redact_summary`）、sentinel 第二路改独立明文「独立第二路」解耦断言（mutation 锁住）、截断正向断言（caplog+`finish_reason`+`[DONE]==1`）、`_mk_sse_event` 统一 `_jdumps` 口径；⑫ design D9 + CHANGELOG v0.9.20；三门禁全绿

- **v0.9.19** — 传输层终审闭环（§9 9.1-9.14，主代理实测 + 3 子任务并行审查 F-01~F-18）：① 9.1 慢链 `byte_buf` 死代码修复（P0）——`del byte_buf[:pos]` 缩进 40/44 落入 `while True` 体内成死代码（5 分支全 `continue/break` 无 fallthrough），`byte_buf` 单调增长至 1MB 粗暴截断 + 正常流误判截断合成，回移 indent36 与快链对称；② 9.2 截断合成去重（P0）——`_build_truncated_event` 不再自带 `[DONE]`，合成前检查已发 DONE（`_done_sent` 透传/合成/补发三处同步置位），截断判定收紧（仅 `byte_buf/data_buffer` 残留）；③ 9.3 choices 全量遍历（P0）——慢链主路径/续行重建改 `for choice in choices` 循环聚合 content/reasoning/tool_calls/finish_reason（原 `choices[0]` 丢 n=2 第二路含 PII 旁路），修复 tool_calls 审计 pending continue 语义；④ 9.4 CR-only 行流末 dispatch（P0）——慢链流末立即分发（原 append 后 clear 丢弃），补 finish_reason/终止类型置位 `seen_global_terminal` + chat 协议 [DONE] 补发防双发；⑤ 9.5 快链 line_buf 行缓冲——快链 content 跨 data 片段合并（`fast_line_buf` + 30s/16KB/候选感知强制），新增 `test_fast_chain_line_buf_cross_data`；⑥ 9.6 响应侧外层 `_shared_validate` 补接（`_pii_response_process_json_aware` 共享 walk/fallback 出口统一校验）；⑦ 9.7 性能缓存——`_token._restore` 显式 mapping 请求级两级缓存（id+len+items 指纹，流内首行编译后续复用）+ `_pii_response_scan` 请求级单槽缓存；⑧ 9.8 `_pii_response_scan` 值级等价改位置区间重叠比较（模型独立输出同值明文不再误跳过掩码）；⑨ 9.9 第四处内联 `_walk` 收敛为共享 `json_walk` 薄包装（消除语义漂移 + RecursionError 兜底 + 外层校验）；⑩ 9.10 三协议文本 delta 同阈值（anthropic/responses 补 30s 强制 flush）；⑪ 9.11 `parsed_obj` 主 dict 路径接线；⑫ 9.12 keepalive 审计盲区——`_ka` 检查 `_audit_approval_pending` 非空即保活（90s 审批不因 inactivity 断流）；⑬ 9.13 测试补强——sentinel_chat fixture 入 CI（choices n=2/tool_calls 真实 handler 回放）、集成断言补 `[DONE] 恰 1` + 无截断、CR-only 真实集成测试（替代自模拟）、tasks 勾选不实标注 5 处；⑭ `_mk_sse_event` 补 `ensure_ascii=False`（中文不再 `\uXXXX` 转义）；464 passed + ruff 零告警 + `openspec validate --strict` 绿

- **v0.9.18** — pii-gateway-hardening 终审闭环（F-01~F-13，§8 8.1-8.13）：① 8.1 `json_walk` 深度守卫真修复——`dict/list` 裸递归漏 `+1` 致 3000 层 `RecursionError`，改 `_impl` 递归 + 入口 `RecursionError` 兜底返回原对象（`depth_limit` 仅统计 `str→inner` 嵌套）；② 8.2 响应期 token 保留语义——`_PII_PARTIAL_TOKEN_RE` 加负向前瞻排除完整形态 `(?![0-9a-f]{8}__)`，`_strip_token_forms` 不再剥离完整 PII（对齐 spec「响应期新 token 不被还原原样保留」），行中残缺前缀同样剥离（`(?=\s|[^\w])`）；③ 8.3 审批消息映射残留清理——`_request_audit_approval` `_ask` 失败时统一 pop pending/msgs/created_ids；④ 8.4 多 `data:` 行无空行安全聚合——聚合 `loads` 失败逐行独立 `_pii_response_process`（不再转发未脱敏原始行）；⑤ 8.5 流末 `pending_cr` 视为行终止符（CR-only 行正常 dispatch 不误报截断）；⑥ 8.6 `PII_HOLD_MAX` 文档修正——仅审计 hold 用，流式超长由 `LINE_BUF_FLUSH=16KB`/`LINE_BUF_MAX_AGE=30s` 控制；⑦ 8.7 新增 `pii_stream_integration_test.py` 真实 handler 集成测试（mock 上游驱动真实 `_handle_openai_sse`/`_handle_anthropic_event`/`_handle_responses_event`，断言真实输出字节）；⑧ 8.8 快链 WHATWG 帧完整性补齐（BOM 单次剥离、`data_buffer` 聚合、`:` 注释透传、CR-only、`_fast_emit_data` 统一 emit）；⑨ 8.10 慢链双重 `json.loads` 合并——`_pii_response_process_json_aware` 加 `parsed_obj` 跳过首层 loads；⑩ 8.11 双累积器合并——删除 `_audit_arg_accum` ContextVar，统一用 `arg_buf`（审计读原始参数须先于 json_aware 还原清空）；⑪ 8.12 sentinel 录制对齐 spec（`choices[1]` 改 token、补 `refusal`/`audio.transcript`）；461 passed + ruff 零告警 + `openspec validate --strict` 绿

- **v0.9.17** — 三层缓冲/keepalive/WHATWG 帧声明（`PII_VAULT_GAP_AWARE` 内置，`SSE_MAX_BUF=1MB`/`LINE_BUF_FLUSH=16KB`/`LINE_BUF_MAX_AGE=30s`/`KEEPALIVE_INTERVAL=10s`）：① 共享 `utils/json_walk.py` 五要素（`orjson`/`BOM`/`depth=5`/`ensure_ascii=False, separators`/`叶子级回退`）+ 三处薄包装（`_token._cred_json_walk` / `_pii._pii_json_walk` / `_llm._pii_response_process_json_aware` + `_pii_process_sse_line` `data:` 前缀剥离后共享 walk，`[DONE]`/空/非 JSON 早退）；② Vault 稳态下标：`register` 收集 `pii_t2p+resp_t2p` 已用 `seq` 空洞跳过（`next_available_index`），`rand8=secrets.token_hex(4)`，`_restore` 仅 `pii_t2p` 命中还原、响应期/未注册原样保留+审计，`PII_FUZZY_RESTORE=0` 默认精确、`1` 时 `re.IGNORECASE`；③ 流式三层缓冲：`byte_buf` WHATWG（`CRLF/LF/CR` 归一、`:` 注释透传、`data_buffer` 同事件 `\n` 聚合、流首 BOM `0xEFBBBF` 单次剥离、`retry` 仅 ASCII 数字、`data:` 单空格剥离、`SSE_MAX_BUF=1MB` 守门）→ `line_buf`（`content/reasoning/refusal` 等文本 delta `while '\n' in buf: restore→_strip_partials→_mk_*_event`，`audio.delta` 忽略，`choices[]` 全量，`16KB/30s` 候选感知强制）→ `arg_buf`（`function_call_arguments.delta`/`partial_json` 等一次性 `json_aware walk`，仅 `finish_reason`/`[DONE]`/`content_block_stop`/`item_done` 时 flush）；④ 截断合成：`!seen_global_terminal && (byte_buf|line_buf|arg_buf/data_buffer)` 时三协议合成终止并 `warning truncated`，`seen_global_terminal` 为全局终止（`[DONE]/finish_reason/content_filter/message_stop/response.completed/failed/incomplete`）、块级 `content_block_stop/item_done` 仅清 `arg_buf`；⑤ 检测硬化总闸 `PII_DETECTION_HARDENING=0/1`（默认关）：保留地址精确前缀（`fc:/fd:` 仅冒号）、`ThreadPoolExecutor(2)+timeout 0.1s`、字典 CJK 边界、`@lru_cache(4)`；新增 `vault_stable_test/residual_hardening_test/detection_hardening_test` + `tests/fixtures/sentinel_{chat,anthropic,responses}.jsonl`（`scripts/sentinel_record.py` 可复现，`keepalive` 可见）

- **v0.9.16** — 流式截断检测+合成终止：`byte_buf` 残留或 `!seen_terminal`（`bytes_written>0 && sse_events>0`）时 `warning`（含 `bytes_buf_len/sse_events/bytes_written/req_id/tail/preview`）并丢弃破损残余，注入协议对应合成终止（`chat→[DONE]` / `anthropic→message_stop` / `responses→response.failed`）避免下游 `JSONDecodeError: char 0`；slow/fast 双路径 `seen_terminal` 追踪（`[DONE]`/`response.completed/failed/incomplete`/`message_stop`/`finish_reason`，fast 含字符串启发式及续行重建路径补标记），`stream_meta.json` 新增 `seen_terminal/truncated/bytes_buf_len`；新增 `TRUNCATED_MESSAGE` 中文提示“请重试”

- **v0.9.15** — C 方案 orjson 全量 json-aware + 叶子级最小回退：移除三处 `len>1M → plain`（`_token._redact/_restore`、`_pii.pii_redact_json_aware`、`_llm._strip_token_forms_json_aware`），大包亦走 `loads→walk→dumps`（`orjson 2-3ms` vs `stdlib 12ms`，`orjson>=3.9` 可选依赖，无则回退 `stdlib`）；`walk` 透传 `path`（`$.a[0].b` / `→$.inner`），叶子级仅 `new_s!=s` 时 `jdumps` 校验失败回退该叶子并 `warning(path)`，其他叶子保留脱敏/还原，泄漏最小；外层二次校验仅 `has_cred/has_pii` / `active_t2p` 非空时触发，`happy path 0 额外 loads`；SSE 按行隔离；新增 `test_orjson_leaf_fallback` 4 场景

- **v0.9.14** — 移除 `>1M` 纯文本回退：`_llm._pii_response_process_json_aware` 与 `_pii_process_sse_line` 及 `_token._restore_json_aware` 的 `len>1M` 守门改走 JSON-aware（大负载仍 `loads→walk→dumps(ensure_ascii=False)` 正确转义 `p@ss"quote`/`\u`/`\`/`\n`/`emoji`），修复 `>1M` 裸 JSON 经 `plain str.replace` 未转义导致 `Expecting ',' delimiter / Invalid \escape` 的 P0 潜伏；`_strip_token_forms_json_aware` 剥离为空安全保留 1M 守卫

- **v0.9.13** — 细化流末 `byte_buf` 残余的 JSON-aware：`data:` 前缀残余走 `SSE 行级` `_pii_process_sse_line`，裸 JSON 片段走 `payload 级` `_pii_response_process_json_aware`，避免 `data: {JSON}` 残余在 `payload 级` 回退 `plain` 时的 `p@ss"quote`/`\u` 破坏；与 `v0.9.12` 的快路径 `data:`/`event:` 修复互补

- **v0.9.12** — 补全剩余流式 JSON-aware 遗漏：`_llm` 快路径 `data:` 载荷与 `event:`/`id:` 非 data 行改走 `_pii_response_process_json_aware`/`_pii_process_sse_line`，`byte_buf` 流末残余双路径（slow/fast）改走 `json-aware` 且残余清理改用 `_strip_token_forms_json_aware`，修复 `p@ss"quote`/`\u`/`\` 在 fast/残余路径的未转义导致 `Expecting value: line 1 column 1 (char 0)` 空体误判；沿用 `len>1M`/`depth>5` 守卫与 `safe/pending` 分割

- **v0.9.11** — 补全增量 JSON-aware 遗漏：`_llm._flush_anthropic_buf/_flush_responses_buf` 与增量 `arg_buf`（`response.function_call_arguments.delta` / `input_json_delta.partial_json`）改走 `_pii_response_process_json_aware`（覆盖 `p@ss"quote`/`\u` 等特殊字符，片段不完整时自动回退 plain 且 safe/pending 分割保持），修复完整 `arg_buf` 的 `{"key":"p@ss"quote"}` 未转义导致 `Expecting ',' delimiter` 闭环；继续沿用 `len>1M` 与 `depth>5` 守卫
- **v0.9.10** — 修复嵌套 JSON 串的脱敏还原破坏：`_token._cred_json_walk` 与 `_pii._pii_json_walk` 对叶字符串内嵌套 JSON 递归 `walk→dumps(ensure_ascii=False)`，覆盖 `tool_calls.arguments` 等 `stringified JSON`；`_llm._pii_process_sse_line` 对 `data: {JSON}` 行做 JSON-aware（含 BOM `\ufeff` 剥离、`[DONE]`/空行早退、`data:[DONE]` 多空格兼容），替换 slow path 的 `data:` 行；非流式由 walk 层覆盖；`separators`/`ensure_ascii` 形态变更属语义等价（非字节级保持）；新增 7 个回归用例
- **v0.9.9** — JSON-aware 热修复：顶层 JSON 叶节点 `loads→walk→dumps`，修复 `\u` 劫持与 `p@ss"quote` 未转义两类 `JSONDecodeError`
- **v0.9.8** — 空 SSE/非 JSON 空体转 502 守门
- **v0.9.7** — 修复 `CREDENTIAL_PROXY_DEBUG_DIR` 对 Responses API 保存失效
- **v0.9.6** — PII 全局持久化 + 并发隔离 + 永不空流